# Check that < and > in resource requests work
NumericResourceRequest

# Check that rusage in resource requests works correctly
ResourceUsageRequest

# Check we can combine resources in a sensible way
MultipleResources
