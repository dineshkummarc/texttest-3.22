# Make sure we can measure CPU time directly via \time
MeasureCpuTime

# Test the option for including system time
IncludeSystemTime

# This format came out on powerpc
AlternativeTimeFormat

# If Gnu time is not installed, you'll probably get something like this
NonGnuTime

# Check we don't get performance if performance_test_machine doesn't match
WrongMachine
HistoricalReport
