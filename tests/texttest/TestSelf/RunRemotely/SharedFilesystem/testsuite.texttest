# Just run a test using rsh
Basic

# Set the machine to run things on in the config file
ChangeDefaultMachine

# Need to make sure we don't hang if ssh wants to ask questions...
UseSSH

# Must set environment variables remotely
EnvironmentVariables

# Make sure we can test performance correctly
PerformanceMeasurement

# What happens if we provide a machine that doesn't exist?
BadMachine

# We need to be able to recognise when we're on the machine that's requested
RecogniseLocalhost
