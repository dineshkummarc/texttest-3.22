# Check that filtering still works after a rename, i.e. that the files get updated with the new path
RenameAndFilter

# Check that filtering still works after a copy, i.e. that the files get updated with the new path
CopyAndFilter

# Check we can save the filtered files, and that we don't get sorting stuff included
SaveFilteredFiles
