# Check we can add a search path for test data
HardCoded

# Check that a search path with several elements is searched in the right order
MultipleElements

# Check we can use the search path on failing to find an environment variable
EnvironmentVariables

# Copy of SearchPathFromEnv, but preset the environment variable as would be the case in SGE/LSF
PresetEnvironment

# Check environment references in the searchpath work correctly
EnvReferences
