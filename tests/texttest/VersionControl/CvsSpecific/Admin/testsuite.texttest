# We have special clauses for CVS to rename to remove directories, make sure the basic bad rename still works
BadRename

# Check we get an appropriate error if the version control tool can't be found
RenameNoInstall
