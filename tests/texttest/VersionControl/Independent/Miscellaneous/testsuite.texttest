# Make sure we can find the right files to analyse from the dynamic GUI
DynamicVersions

# See if we can get CVS information out of a running test
RunningTest

# Add a new application, make sure it doesn't end up unusable...
AddApplication
